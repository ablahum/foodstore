import styled from 'styled-components'

export const Title = styled.h2`
  color: var(--muted);
  font-size: 1rem;
  margin: 0;
  margin-right: 1em;

  @media screen and (max-width: 991px) {
    display: none;
  }
`
